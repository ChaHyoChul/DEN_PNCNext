# 실시간 상태 저장 파이프라인 구현 내역 정리

**작성일**: 2026-05-15  
**주제**: PncNext 프로젝트 - 하드웨어 상태 데이터의 실시간 전역 동기화 구현

## 1. 개요
모션 제어기로부터 수신되는 복합 데이터(RND_CDT)를 실시간으로 파싱하고, 이를 프로그램 전역에서 사용할 수 있도록 상태 저장소(State Store)에 반영하는 파이프라인을 구축했습니다.

## 2. 주요 구현 사항

### 2.1. 싱글톤 상태 저장소 (`IMotionStateStore`) 도입
- **목적**: 장비의 최신 원시 데이터를 메모리에 유지하고 모든 서비스 및 API에 제공.
- **클래스**: `MotionStateStore` (Singleton)
- **특징**: `PAMotionControllerState` 객체를 내부적으로 소유하며, 하드웨어에서 읽어온 최신 정보를 안전하게 보관합니다.

### 2.2. 채널 인터페이스 확장 (`IMotionChannel`)
- **목적**: 전송 계층과 프로토콜 계층의 유연한 협력을 지원.
- **추가 기능**:
  - `ReceiveRawAsync()`: 프로토콜 해석 전의 원시 바이트 데이터를 직접 획득하는 기능.
  - `Protocol` 속성 노출: 현재 채널이 사용하는 구체적인 프로토콜 객체에 접근 가능하도록 함.

### 2.3. 제어 서비스 로직 고도화 (`PAMotionControlService`)
- **변경 전**: `GetStatusAsync()` 호출 시 하드웨어의 실행 상태(`MotionStatus`)만 반환하고 상세 데이터는 소멸됨.
- **변경 후**: 
  1. 하드웨어로부터 원시 응답 전체를 수신.
  2. 수신된 데이터를 `PAMotionProtocol.UpdateStateFromResponse`에 전달하여 상세 상태 객체(`PaState`) 최신화.
  3. 도메인 공통 상태값 반환.

### 2.4. 의존성 주입(DI) 및 동적 팩토리 고도화
- `Program.cs`에서 `MotionStateStore`를 싱글톤으로 등록.
- 제어 서비스 생성 시 상태 저장소를 자동으로 주입받도록 팩토리 로직을 업데이트하여 런타임 안정성 확보.

## 3. 데이터 흐름 (Data Flow)
1. **요청**: 백그라운드 서비스가 `IMotionControl.GetStatusAsync()` 주기적 호출.
2. **수신**: 제어 서비스가 하드웨어 응답(byte[])을 받음.
3. **분석**: 프로토콜 클래스가 응답을 파싱하여 `PAMotionControllerState` 필드들을 채움.
4. **저장**: 분석된 결과가 전역 싱글톤 저장소에 반영됨.
5. **사용**: UI(WPF/Web)는 저장소의 데이터를 즉시 읽어 실시간 좌표 및 알람 표시.

## 4. 결론
이로써 하드웨어와 소프트웨어 모델 간의 데이터 동기화 체계가 완성되었습니다. 장비의 사소한 상태 변화도 즉시 시스템 상태에 반영되며, 이는 향후 고성능 모니터링 및 인터락(Interlock) 시스템의 기반이 됩니다.
