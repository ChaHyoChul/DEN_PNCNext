# 비동기 통신 및 고효율 데이터 관리 전략 분석서

**작성일**: 2026-05-08  
**주제**: PncNext 프로젝트 - 비동기 수신 루프 및 상태 저장소(State Store) 패턴

## 1. 개요
덴탈 밀링머신의 실시간 제어와 모니터링을 위해 기존의 동기식(Request-Response) 통신을 비동기 이벤트 기반 아키텍처로 전환하고, 시스템 오버헤드를 최소화하면서 데이터의 실시간성을 보장하는 전략을 정의합니다.

## 2. 비동기 통신 엔진 구조

### 2.1. 백그라운드 수신 루프 (Background Receiver Loop)
- **방식**: 통신 포트 오픈 시 별도의 전용 태스크(`ReadLoopAsync`)를 실행하여 하드웨어로부터 들어오는 바이트 스트림을 실시간으로 읽어들입니다.
- **장점**: 장비가 일방적으로 보내는 알람(Unsolicited Message)이나 실시간 좌표 데이터를 지연 없이 수신할 수 있습니다.

### 2.2. 메시지 채널 (System.Threading.Channels) 활용
- **역할**: 수신 루프(생산자)와 파싱 로직(소비자) 사이의 완충 지대 역할을 수행합니다.
- **특징**: Lock-free 구조로 설계되어 다중 스레드 환경에서 매우 높은 성능을 내며, 수신된 데이터를 순차적으로 안전하게 처리합니다.

## 3. 중복 명령 전송 방지 및 요청 매칭

비동기 환경에서 동일한 명령이 중복 전송되는 것을 막기 위해 다음 전략을 사용합니다.

### 3.1. Task 재사용 (In-Flight Task Tracking)
- 특정 명령(예: 상태 조회)이 진행 중일 때 동일한 요청이 들어오면, 새로운 명령을 생성하지 않고 **기존에 진행 중인 Task를 반환**하여 대기시킵니다.
- 응답이 도착하면 대기하던 모든 호출자가 동시에 결과를 공유받습니다.

### 3.2. 타임아웃 메커니즘
- 제어기 응답이 유실될 경우를 대비하여 모든 비동기 대기 작업에 `CancellationToken` 기반의 타임아웃(예: 3~5초)을 설정하여 시스템 데드락을 방지합니다.

## 4. 상태 저장소(State Store) 패턴을 통한 최적화

### 4.1. 생산자-소비자 분리
- **생산자(Poller)**: 백그라운드 서비스가 100ms 주기로 제어기 상태를 읽어 싱글톤 저장소인 `MotionStateStore`를 갱신합니다.
- **소비자(UI/API)**: 제어기에 직접 묻지 않고 `MotionStateStore`에 이미 저장된 최신 값을 즉시 읽어갑니다.

### 4.2. 성능 이점
- **오버헤드 최소화**: 초당 10회 정도의 `Task` 생성은 현대 .NET 환경에서 무시할 수 있는 수준이지만, 저장소 패턴을 통해 실제 생성되는 `Task` 수를 획기적으로 줄여 CPU 및 메모리 부하를 방지합니다.
- **응답성 향상**: UI 요청 시 하드웨어 통신 대기 시간 없이 즉시 데이터 반환이 가능합니다.

## 5. 구현 시 고려 사항

### 5.1. ValueTask 활용
- 상태가 빈번하게 조회되거나 데이터가 이미 준비되어 있는 경우, 힙 할당을 줄이기 위해 `ValueTask` 사용을 검토합니다.

### 5.2. 스레드 안전성
- 싱글톤 상태 객체에 대한 동시 접근 시 데이터 무결성을 보장하기 위해 `lock` 또는 `Interlocked` 클래스를 사용합니다.

## 6. 결론
"백그라운드 수신 루프 + 채널 + 상태 저장소" 구조는 장비 제어의 실시간성과 시스템 안정성을 동시에 확보할 수 있는 최적의 아키텍처입니다. 이 구조를 통해 하드웨어의 부하를 줄이면서도 사용자에게는 끊김 없는 모니터링 환경을 제공할 수 있습니다.
