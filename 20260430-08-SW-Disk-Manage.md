# 디스크(Disk) 소재 관리 및 가공 영역 시각화 분석서

**작성일**: 2026-04-30  
**대상**: PncNext 프로젝트 소재 관리 및 가공 레이아웃 시각화 모듈

## 1. 개요
덴탈 가공의 효율성을 높이고 소재 낭비를 줄이기 위해서는 사용 중인 디스크(Disk)의 잔여 영역을 정확히 파악해야 합니다. 본 문서에서는 디스크의 등록, 가공 이력에 따른 영역 관리, 그리고 UI 상에서의 시각적 레이아웃 표시를 위한 데이터 구조와 프로그램 아키텍처를 분석합니다.

## 2. 데이터 구조 설계 (DB)

### 2.1. Disks (디스크 마스터 정보)
사용할 치과용 디스크 소재의 기본 정보를 관리합니다.
- `Id` (PK)
- `DiskId`: 디스크 고유 식별자 (바코드 또는 사용자 지정 ID)
- `MaterialType`: 소재 종류 (Zirconia, Wax, PMMA, Titanium 등)
- `Diameter`: 디스크 지경 (일반적으로 98mm)
- `Thickness`: 두께 (10T, 14T, 20T 등)
- `LotNumber`: 제조 로트 번호 (품질 관리용)
- `Status`: 상태 (New, InUse, Completed)

### 2.2. DiskWorkAreas (가공 영역 이력)
특정 디스크에서 이미 가공되었거나 가공될 영역의 좌표 정보를 저장합니다.
- `Id` (PK)
- `DiskId`: 해당 디스크의 식별자 (FK to Disks)
- `JobId`: 관련 가공 작업 ID (FK to JobHistory)
- `NcFileName`: 가공에 사용된 NC 파일명
- `CenterX`, `CenterY`: 디스크 중심 기준 가공 중심 좌표
- `Width`, `Height`: 가공 영역의 크기 (또는 외곽 반경)
- `Color`: UI 표시 색상 (과거 이력과 현재 작업 구분용)
- `ProcessedDate`: 가공 일시

## 3. 프로그램 모듈 구조

### 3.1. DiskManagementService (소재 관리 모듈)
- **디스크 등록/조회**: 신규 디스크 정보 등록 및 현재 사용 가능한 디스크 목록 제공.
- **이력 통합**: 특정 디스크를 선택했을 때 해당 디스크의 모든 `DiskWorkAreas`를 로드.

### 3.2. NC Metadata Parser (NC 분석 모듈)
- **정보 추출**: NC 파일 로딩 시 주석(Comments)이나 메타데이터 섹션에서 사용할 `DiskId` 및 해당 작업의 `WorkArea` 좌표 정보를 추출합니다.
  - 예: `;DISK_ID: ZIR-001`, `;WORK_AREA: X10.5 Y15.2 R12.5`

### 3.3. DiskVisualizationEngine (시각화 엔진)
- **좌표 계산**: 디스크 크기와 각 가공 영역의 좌표를 기반으로 UI(WPF/Web)에 그릴 수 있는 그래픽 데이터를 생성합니다.
- **레이어 관리**:
    - **Base Layer**: 디스크 외형 (원형)
    - **History Layer**: 이전에 가공 완료된 영역 (회색 또는 반투명 표시)
    - **Active Layer**: 이번에 가공할 현재 NC 파일의 영역 (강조 색상 표시 - 예: 파란색 또는 노란색)

## 4. 핵심 워크플로우

### 4.1. NC 파일 로딩 및 디스크 매칭
1. 사용자가 NC 파일 로드.
2. `NC Parser`가 파일 내부에서 `DiskId`와 가공 위치 정보 추출.
3. `DiskManagementService`가 DB에서 해당 `DiskId`의 정보와 이전 가공 이력(`DiskWorkAreas`)을 모두 조회.
4. `VisualizationEngine`이 조회된 모든 영역을 캔버스 상에 배치.

### 4.2. 가공 영역 충돌 방지 (검토 사항)
- 신규 가공 영역이 이전 이력 영역과 겹치는지 체크하는 로직을 추가하여 소재 파손 및 가공 실패 사전 방지.

## 5. 실시간 데이터 공유 (ThreadState 연동)
- 현재 로딩된 디스크 정보와 가공 영역 데이터를 `ThreadState`에 포함시켜, WPF 클라이언트의 메인 화면이나 Web App의 모니터링 페이지에서 디스크 상태를 실시간으로 시각화합니다.

## 6. 결론 및 향후 고려 사항
- **3D 시각화 확장**: 현재는 2D 평면 레이아웃 위주이나, 디스크 두께(Thickness)와 가공 깊이를 고려한 3D 시각화로 확장 가능성이 있습니다.
- **소재 잔량 자동 계산**: 가공된 영역을 제외한 가용 면적을 수치로 계산하여 사용자에게 제공.
- **QR/Barcode 연동**: 카메라를 통한 디스크 자동 인식 기능 추가 검토.

이 설계를 통해 소재의 사용 현황을 직관적으로 파악하고, 가공 실수를 방지하며 소재 활용도를 극대화할 수 있는 관리 시스템을 구축할 수 있습니다.
