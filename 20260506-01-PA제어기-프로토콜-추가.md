# 2026-05-06 PA 제어기 프로토콜 고도화 및 시스템 재편 작업 내역

오늘 PA 모션 컨트롤러의 실제 통신 규격(PAAsyncComm.md)을 반영하여 통신 레이어를 정교화하고, 다중 제어기 지원을 위한 시스템 구조 재편 작업을 완료했습니다.

## 1. PA 제어기 실 규격 기반 통신 고도화
- **TcpCommPort 개선**: 
  - 메시지 종결자(`\r\n`) 기반의 수신 버퍼링 로직 구현.
  - 비동기 스트림 처리 시 데이터 유실 방지를 위한 라인 단위 추출 기능 추가.
- **PAMotionProtocol 정교화**:
  - 기존 임시 제어 문자(`STX`/`ETX`)를 실제 규격인 CRLF 종결자로 변경.
  - `MOV`, `RND_STOP`, `RND_STATUS` 등 실제 명령어 포맷 적용.
  - `InvariantCulture` 및 `F3` 포맷을 적용하여 전송 데이터 정밀도 및 호환성 확보.
  - `RND_STATUS` 응답 메시지 내 프로그램 상태(PS) 파싱 로직 기초 구현.

## 2. 다중 제어기 지원 및 구조 재편
- **인프라 레이어 구조화**: `Infrastructure/Motion` 하위에 `Protocols`, `Transports`, `Services` 폴더를 생성하여 역할별로 파일 재배치.
- **INTH 프로토콜 추가**: `INTHMotionProtocol` 구현체를 추가하여 향후 INTH 제어기 도입을 위한 기반 마련.
- **서비스 분리**: 기존 모션 서비스를 `PAMotionControlService`와 `INTHMotionControlService`로 세분화.

## 3. 동적 객체 생성 및 의존성 주입(DI) 강화
- **Dynamic DI Factory 적용**: `Program.cs`에서 하드코딩된 서비스 등록을 제거하고, DB(`MotionControllerConfigs`) 설정을 읽어 런타임에 통신 방식과 프로토콜을 조합하여 객체를 생성하는 팩토리 로직 적용.
- **유연한 확장성**: 이제 DB 설정 변경만으로 장비의 제어기 종류나 통신 방식을 즉시 전환할 수 있습니다.

## 4. 실시간 데이터 공유 기반 구축
- **Shared Memory (MMF)**: `SharedMemoryService`와 `MotionSharedData` 구조체를 통해 WPF 클라이언트와 고속으로 좌표 데이터를 공유할 수 있는 채널 구축.

---
**주요 완료 파일:**
- `Infrastructure/Motion/Protocols/PA/PAMotionProtocol.cs`
- `Infrastructure/Motion/Protocols/INTH/INTHMotionProtocol.cs`
- `Infrastructure/Motion/Transports/TcpCommPort.cs`
- `Infrastructure/Motion/Services/PAMotionControlService.cs`
- `Infrastructure/Motion/Services/INTHMotionControlService.cs`
- `Program.cs` (동적 DI 로직 반영)
