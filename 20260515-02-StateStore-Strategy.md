# 다중 제어기 상태 관리 통합 전략 분석서

**작성일**: 2026-05-15  
**주제**: PncNext 프로젝트 - 통합 상태 저장소(Unified State Store) 설계 및 확장 방안

## 1. 개요
PncNext 프로젝트는 PA 제어기와 INTH 제어기를 모두 지원해야 합니다. 각 제어기에서 수신되는 상세 상태 데이터를 어떻게 메모리에 유지하고 관리할지에 대한 전략을 정의합니다. 본 문서는 '방안 1: 통합 저장소 방식'을 기반으로 한 설계 내용을 담고 있습니다.

## 2. 채택된 전략: 통합 저장소 방식 (Unified State Store)

모든 종류의 제어기 상태 객체를 하나의 싱글톤 서비스인 `MotionStateStore`에서 통합 관리하는 방식을 채택합니다.

### 2.1. 구조적 특징
- **단일 진입점**: 프로그램의 어떤 모듈에서도 `IMotionStateStore` 하나만 주입받으면 모든 제어기의 상세 상태에 접근할 수 있습니다.
- **객체 공존**: `PaState`, `InthState` 등의 고유 상태 객체들이 저장소 내에 독립적인 프로퍼티로 존재합니다.

### 2.2. 장점 (선택 이유)
1. **데이터 접근의 편의성**: UI API나 백그라운드 서비스가 현재 활성화된 장비가 무엇인지 매번 판단할 필요 없이, 저장소에서 제공하는 프로퍼티를 즉시 참조할 수 있습니다.
2. **공유 메모리(MMF) 연동 효율성**: `SharedMemoryService`가 각기 다른 제어기 데이터를 표준 구조체(`MotionSharedData`)로 복사할 때, 데이터 소스가 한곳에 모여 있어 매핑 로직이 단순해집니다.
3. **관제의 일관성**: 장비의 물리적 교체나 시뮬레이션 모드 전환 시에도 상태 관리 인터페이스가 동일하게 유지되어 상위 레이어의 코드 수정을 최소화합니다.

## 3. 세부 설계 및 확장 계획

### 3.1. 인터페이스 확장 (`IMotionStateStore`)
기존 PA 중심의 구조에서 INTH 및 공통 기능을 수용하도록 확장합니다.
- `PaState`: PA 제어기 전용 상세 데이터.
- `InthState`: INTH 제어기 전용 상세 데이터 (추가 예정).
- `UpdateSharedData()`: 현재 활성화된 제어기 데이터를 `MotionSharedData` 형식으로 변환하여 반환하거나 업데이트하는 공통 메서드.

### 3.2. 데이터 동기화 흐름
1. **각 제어기 서비스 (`PAMotionControlService` 등)**: 자신의 프로토콜로 데이터를 읽어와서 저장소 내의 대응되는 상태 객체(`PaState` 등)를 갱신합니다.
2. **저장소 (`MotionStateStore`)**: 데이터 갱신 시 `NotifyStateChanged()`를 통해 변경 사실을 알립니다.
3. **공유 메모리 서비스 (`SharedMemoryService`)**: 변경 알림을 받으면 저장소의 최신 값을 읽어 공유 메모리에 기록합니다.

## 4. 결론
'통합 저장소 방식'은 제어기별 고유 특성을 온전히 보존하면서도 시스템 전체의 데이터 흐름을 단순하고 견고하게 유지할 수 있는 최적의 선택입니다. 향후 INTH 제어기 기능 추가 시, 이 구조에 따라 `INTHMotionControllerState`를 추가하고 매핑 로직만 보완함으로써 신속한 확장이 가능합니다.
