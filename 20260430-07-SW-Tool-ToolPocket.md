# 툴(Tool) 및 툴 포켓(Tool Pocket) 관리 시스템 분석서

**작성일**: 2026-04-30  
**대상**: PncNext 프로젝트 툴 수명 관리 및 이력 추적 모듈

## 1. 개요
덴탈 밀링머신의 정밀도 유지와 무인 가공을 위해서는 사용되는 툴의 수명을 정확히 관리하고, 에러 발생 시 백업 툴을 자동으로 선택하는 지능형 툴 관리 시스템이 필수적입니다. 본 문서에서는 툴의 생애주기 관리와 데이터 구조, 그리고 가공 이력과의 연동 방안을 분석합니다.

## 2. 데이터 구조 설계 (DB)

### 2.1. ToolPockets (툴 포켓 설정)
포켓 자체의 물리적 위치와 할당된 사양을 관리합니다.
- `Id` (PK)
- `PocketNumber`: 포켓 번호 (1, 2, 3...)
- `TargetDiameter`: 해당 포켓에 할당된 툴 직경 (1.0, 1.5, 0.6 등)
- `CurrentToolId`: 현재 장착된 툴의 ID (FK to Tools)
- `IsActive`: 포켓 사용 여부

### 2.2. Tools (개별 툴 정보)
교체 시마다 생성되는 개별 툴의 마스터 데이터입니다.
- `ToolId` (PK): "직경-교체날짜-시간" (예: 1.0-20260430-1430)
- `Diameter`: 직경
- `MaxUsageTime`: 최대 사용 가능 시간 (분)
- `UsedTime`: 현재까지 사용한 누적 시간 (분)
- `Status`: 툴 상태 (Normal, Timeout, Broken, LengthError)
- `ErrorCode`: 상태가 에러일 경우의 상세 코드
- `BackupToolId`: 해당 툴 사용 불가 시 대체할 백업 툴의 ID (FK to Tools)
- `InstallDate`: 툴 장착 일시

### 2.3. ToolUsageHistory (가공 이력 연동)
특정 가공 작업 시 어떤 툴을 얼마나 사용했는지 기록합니다.
- `Id` (PK)
- `JobId`: 가공 작업 ID (FK to JobHistory)
- `ToolId`: 사용된 툴 ID (FK to Tools)
- `UsageDuration`: 해당 작업에서 사용된 시간 (분)

## 3. 프로그램 모듈 구조

### 3.1. ToolService (툴 생명주기 관리)
- **자동 ID 생성**: 툴 교체 시 현재 시간과 직경을 조합하여 고유 `ToolId` 생성.
- **상태 업데이트**: 가공 중 실시간 사용 시간 누적 및 수명 초과(Timeout) 판단.
- **에러 핸들링**: 툴 파손이나 측정 에러 발생 시 상태 및 에러 코드 기록.

### 3.2. ToolSelectionLogic (백업 툴 엔진)
- 가공 시작 전 NC 파일이 요구하는 툴 직경을 확인.
- 해당 포켓의 툴이 `Error` 또는 `Timeout` 상태인 경우, `BackupToolId`에 등록된 툴을 자동으로 선택하는 로직 수행.

### 3.3. JobExecutionObserver (이력 추적기)
- 가공 완료 시 현재 가공된 Disk 정보와 사용된 모든 툴의 정보를 매핑하여 최종 가공 리포트 생성.

## 4. 핵심 워크플로우

### 4.1. 툴 교체 시나리오
1. 사용자가 특정 포켓의 툴 교체 명령 수행.
2. 시스템이 `TargetDiameter` 확인 후 `1.0-20260430-1500` 형식의 새 `ToolId` 생성.
3. 기존 툴은 이력으로 남기고, 새 툴 데이터를 DB에 추가 및 포켓에 할당.

### 4.2. 가공 중 에러 및 백업 전환
1. 가공 중 툴 파손 감지.
2. 해당 툴 상태를 `Broken`으로 변경.
3. 시퀀스 엔진이 가공을 일시 중단하고, 동일 직경의 `BackupTool`이 있는지 확인.
4. 백업 툴이 있다면 자동 툴 교체(ATC) 수행 후 가공 재개.

## 5. 실시간 데이터 공유 (ThreadState 연동)
- `ThreadState` 객체 내에 `CurrentTools` 배열을 포함하여 UI(WPF/Web)에서 실시간으로 각 포켓의 툴 상태와 잔여 수명을 시각화(프로그래스 바 등)할 수 있도록 공유 메모리 및 SignalR로 전파합니다.

## 6. 결론 및 향후 고려 사항
- **통합 조회 기능**: NC 파일 정보 + Disk 정보 + 사용 툴 정보를 한눈에 볼 수 있는 가공 완료 리포트 API 및 UI 설계.
- **툴 수명 최적화**: 단순히 시간 기반이 아닌 가공 부하를 고려한 수명 계산 로직으로 확장 가능성.
- **사용자 알림**: 툴 수명이 10% 미만으로 남았을 때 사전 경고 알림 기능 추가.

이와 같은 구조를 통해 툴의 시작부터 폐기까지 전 과정을 정밀하게 관리하고, 데이터 기반의 효율적인 장비 운영 환경을 구축할 수 있습니다.
