# PA 제어기 전용 프로토콜(RND_CDT) 파싱 전략 분석서

**작성일**: 2026-05-14  
**주제**: PncNext 프로젝트 - RND_CDT 응답 처리 및 아키텍처 설계

## 1. 개요
PA 모션 컨트롤러에서 제공하는 `RND_CDT` (Read Combined Data) 명령은 장비의 모든 핵심 상태(좌표, I/O, 스핀들, 에러 등)를 하나의 메시지로 반환하는 고효율 명령입니다. 본 문서는 이 명령의 응답을 파싱하여 시스템 상태에 반영하기 위한 최적의 구조와 세부 로직을 정의합니다.

## 2. 아키텍처 설계 원칙

### 2.1. 인터페이스 순수성 유지 (Interface Purity)
- `IMotionProtocol` 인터페이스에는 `RND_CDT` 관련 메서드를 추가하지 않습니다.
- **이유**: `RND_CDT`는 PA 제어기만의 특화된 기능이므로, 범용 인터페이스에 포함할 경우 다른 제어기(INTH, Dummy 등) 구현 시 불필요한 의존성이 생기는 '인터페이스 오염'을 방지하기 위함입니다.

### 2.2. 정보 은닉 및 캡슐화 (Encapsulation)
- 모든 상세 파싱 로직은 `PAMotionProtocol` 클래스 내부에 `private` 메서드로 구현합니다.
- 외부(Service Layer)에는 상태 객체를 주입받아 업데이트해 주는 전용 메서드만 노출하거나, 기존 인터페이스 메서드를 확장 구현합니다.

## 3. RND_CDT 응답 파싱 상세 규격

### 3.1. 메시지 구조
`RND_CDT [Field0],[Field1],...,[Field20]\r\n` (총 21개 필드)

### 3.2. 주요 필드별 파싱 규칙
1.  **프로그램 상태 (Index 1)**: `PS:File:B:L:E:R`
    - `:` 구분자로 재분리하여 라인 번호, 에러 코드, 실행 상태 추출.
2.  **축 좌표 (Index 2~7)**: `X:pos` ~ `C:pos`
    - 각 축의 식별자를 확인하고 부동 소수점 데이터 추출.
3.  **서보 상태 (Index 8)**: `SV:P:H:E` (Power, Home, Error)
    - 각 비트별로 `IsServoOn`, `IsHomComplete` 등에 매핑.
4.  **입출력 상태 (Index 15~18)**: Hex 문자열 (2~8 chars)
    - 16진수 문자열을 정수로 변환 후 비트 연산을 통해 `Input[]`, `Output[]` 배열 업데이트.

### 3.3. 상태 비트마스크 적용 (Index 19)
제공된 비트마스크 테이블을 적용하여 다음 12가지 세부 플래그를 결정합니다.
- `0x0004`: 스핀들 동작 상태 (`IsSpindleRun`)
- `0x0010`: 툴 체인지 진행 여부 (`IsDuringToolChange`)
- `0x0020`: EMO 버튼 눌림 여부 (`IsEmoButtonPressed`)
- `0x0040`: 모터 이동 여부 (`IsMotorMoving`)
- 기타 공압 충전, 툴 길이 업데이트 필요 여부 등.

## 4. 프로그램 수정 방향

### 4.1. PAMotionProtocol 클래스 확장
- `private void ParseRndCdt(string payload, PAMotionControllerState state)`: 문자열 기반의 실제 파싱 엔진.
- `public void UpdateStateFromResponse(byte[] response, PAMotionControllerState state)`: 서비스 레이어에서 호출할 전용 진입점.

### 4.2. 데이터 타입 안전성
- `double.Parse` 및 `int.Parse` 시 `CultureInfo.InvariantCulture`를 사용하여 소수점 유실 및 지역화 오류 방지.
- 예외 처리 로직을 포함하여 잘못된 응답 수신 시 시스템 안정성 유지.

## 5. 결론
이 설계를 통해 PA 제어기의 강력한 기능을 십분 활용하면서도, 전체 시스템 아키텍처는 유연하고 깔끔하게 유지할 수 있습니다. 각 채널이 독립적인 프로토콜을 가진 현재 구조에서 PA 채널만이 이 특화된 파싱 로직을 수행하게 됩니다.
