# 프로그램 전체 상태(ThreadState) 및 모듈별 데이터 통합 관리 방안 분석서

**작성일**: 2026-04-30  
**대상**: PncNext 프로젝트 소프트웨어 상태 관리 및 실시간 데이터 동기화 모듈

## 1. 개요
덴탈 밀링머신의 안정적인 가공과 정밀한 제어를 위해서는 하드웨어(제어기)의 상태뿐만 아니라 소프트웨어의 가공 프로세스 상태, 작업 정보, 주변 장치(Autoloader) 상태 등을 포함하는 **전체 프로그램 상태(ThreadState)**를 통합 관리해야 합니다. 본 문서에서는 각 모듈별 데이터 저장 방식과 이를 실시간으로 공유하기 위한 아키텍처를 분석합니다.

## 2. 프로그램 상태(ThreadState)의 구성 요소

### 2.1. 장비 프로세스 상태 (Machine Process State)
- **Ready**: 가공 준비 완료 상태.
- **ToRun / Run**: 가공 시작 중 및 가공 진행 중.
- **ToStop / Stopped**: 정지 중 및 정지 완료.
- **Init**: 초기화 진행 중.
- **Error**: 장비 또는 소프트웨어 오류 발생.
*※ 제어기의 물리적 상태(Idle, Running 등)와 연동되어 소프트웨어 로직에 의해 결정됩니다.*

### 2.2. 가공 작업 정보 (Job Information)
- **NC 파일명**: 현재 가공 중이거나 대기 중인 파일 경로 및 이름.
- **가공 진척도**: 전체 라인 수 대비 현재 실행 라인 수.
- **남은 시간**: 예상 소요 시간 및 경과 시간.

### 2.3. 시스템 및 주변 장치 정보 (Peripheral Info)
- **Disk 정보**: 로그 저장 및 NC 파일 보관을 위한 디스크 잔량.
- **Autoloader 슬롯 정보**: 각 슬롯의 재료 유무, 재료 종류, 상태(가공 완료 여부) 등.
- **도구(Tool) 정보**: 현재 장착된 툴의 번호 및 수명 정보.

## 3. 모듈별 데이터 저장 및 관리 구조

### 3.1. 분산 저장 및 중앙 집약 모델 (Distributed Storage & Central Aggregation)
각 모듈은 자신의 도메인 데이터를 독립적으로 관리하되, 외부 공유가 필요한 데이터는 중앙의 **Global State Manager**를 통해 동기화합니다.

| 모듈 명 | 담당 데이터 | 관리 방식 |
| :--- | :--- | :--- |
| **Motion Module** | 제어기 상태, 좌표 | 실시간 폴링 및 즉각 갱신 |
| **Job Module** | NC 파일, 가공 시퀀스 | 파일 스트리밍 상태 관리 |
| **System Module** | 디스크, 하드웨어 정보 | 주기적 체크 (저주기) |
| **Peripheral Module**| 오토로더, 슬롯 상태 | 이벤트 발생 시 갱신 |

## 4. 실시간 데이터 공유 아키텍처

### 4.1. 서버 내부: Mediator 패턴 기반 통합 상태 관리
- **State Store (Singleton)**: 모든 모듈의 핵심 데이터를 모아놓은 `ThreadState` 싱글톤 객체.
- **State Dispatcher**: 상태가 변경될 때마다 등록된 리스너(SignalR, Shared Memory 등)에게 변경 사항을 전파.

### 4.2. 외부 클라이언트 공유 (Web & WPF)

#### **A. 공유 메모리 (MemoryMappedFiles) 확장**
- **대상**: WPF 클라이언트
- **방식**: 기존 제어기 데이터(좌표 등) 외에 `ThreadState` 구조체를 공유 메모리에 추가 정의.
- **구조**: `[ControllerData(Fixed)] + [ThreadState(Fixed)] + [AutoloaderSlots(Array)]`
- **장점**: 수 밀리초(ms) 단위의 초고속 반영.

#### **B. SignalR (WebSockets) - 상태 변경 알림**
- **대상**: Web App (React) 및 관제 시스템
- **방식**: 상태값이 변경되는 이벤트가 발생할 때만 클라이언트에 JSON 형태로 전송.
- **장점**: 네트워크 부하 최소화 및 실시간 이벤트 대응.

## 5. 데이터 업데이트 시퀀스 및 정합성 보장

1.  **데이터 수집**: `MotionStatusBackgroundService` 또는 각 모듈별 감시 서비스가 데이터 획득.
2.  **상태 판단**: 수집된 데이터를 바탕으로 현재 `MachineState`를 결정 (예: 제어기가 Error이면 `ThreadState`도 Error로 전이).
3.  **저장**: 중앙 `ThreadState` 객체 갱신 (Thread-safe lock 활용).
4.  **공유**: 갱신된 내용을 공유 메모리에 쓰고, SignalR 허브를 통해 웹 클라이언트에 브로드캐스트.

## 6. 결론 및 향후 구현 가이드
- **Blittable 구조체 설계**: 공유 메모리 사용을 위해 `ThreadState`를 고정 크기의 구조체로 설계해야 합니다. (문자열은 고정 길이 char array로 처리)
- **모듈 간 디커플링**: 각 모듈은 서로의 내부 로직을 알 필요 없이 중앙의 `State Manager`만 바라보고 데이터를 업데이트하도록 설계하여 유지보수성을 높입니다.
- **상태 전이 매트릭스 정의**: `ToRun -> Run`과 같은 상태 전이가 올바른 조건(예: 문 닫힘 확인, 제어기 연결 확인)에서만 일어나도록 엄격한 상태 관리 로직이 필요합니다.

본 분석을 바탕으로, 프로그램의 복잡도가 높아져도 데이터의 일관성과 실시간성을 유지할 수 있는 견고한 상태 관리 시스템을 구축할 수 있습니다.
