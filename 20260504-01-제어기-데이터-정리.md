# 2026-05-04 제어기 데이터 관리 및 실시간 공유 아키텍처 정리

오늘 PncNext 프로젝트에서 다양한 제어기를 지원하고, 실시간 데이터를 WPF 등 외부 클라이언트와 고속으로 공유하기 위한 아키텍처 고도화 작업을 진행했습니다.

## 1. 제어기 상태 데이터 정의
- **PAMotionControllerState**: "PA 모션 컨트롤러 상태 데이터 정의.txt" 내용을 바탕으로 6축 좌표, I/O 상태(64포트), 스핀들 정보, 에러 코드 등을 포함하는 상세 상태 클래스를 구현했습니다.
- **위치**: `PncNext.Domain/Models/PAMotionControllerState.cs`

## 2. 다중 제어기 지원을 위한 구조 재편
향후 PA 제어기 외에 INTH 제어기 등을 유연하게 추가할 수 있도록 `Infrastructure` 레이어의 구조를 계층화했습니다.
- **Protocols**: `PA`, `INTH`, `Dummy` 전용 프로토콜 로직 분리.
- **Transports**: TCP, Serial 등 공통 통신 계층 관리.
- **Services**: `PAMotionControlService`, `INTHMotionControlService`로 구체화된 제어 로직 구현.

## 3. 실시간 데이터 공유 (Shared Memory)
WPF 클라이언트와의 초고속 데이터 연동을 위해 Memory-Mapped File 기술을 적용했습니다.
- **MotionSharedData**: 6축 위치 데이터를 포함하는 Blittable 구조체 정의.
- **SharedMemoryService**: `PncNext_MotionData` 공유 메모리 자원을 관리하며, 데이터를 쓰거나 읽는 기능을 제공 (Singleton으로 등록).

## 4. DB 기반 동적 제어기 생성 (Factory Pattern)
`Program.cs`의 DI 등록 방식을 개선하여, 애플리케이션 실행 시 DB(`MotionControllerConfigs`) 설정을 읽어 적절한 객체를 자동으로 조립하도록 구현했습니다.
- **동적 조합**: `CommType`(Ethernet/Serial), `ProtocolProvider`(PA/INTH/Dummy) 설정에 따라 런타임에 객체 주입.
- **유연성**: 코드 수정 없이 DB 설정 변경만으로 실제 장비 모드와 시뮬레이션 모드를 전환할 수 있습니다.

## 5. 향후 데이터 흐름 최적화 제안
현재 구조를 더욱 견고하게 만들기 위해 다음과 같은 방향을 제시했습니다.
- **MotionStateStore (Singleton)**: 하드웨어에서 읽어온 원시 데이터를 안전하게 보관하는 전역 저장소 도입.
- **데이터 허브 역할**: `StateStore`에 데이터가 업데이트되면, 이를 필요한 형태(WPF용 MMF, Web용 SignalR)로 가공하여 전파하는 중앙 집중식 흐름 구축.

---
**오늘 완료된 주요 파일:**
- `PAMotionControllerState.cs`
- `INTHMotionProtocol.cs`
- `SharedMemoryService.cs` / `MotionSharedData.cs`
- `PAMotionControlService.cs` / `INTHMotionControlService.cs`
- 업데이트된 `Program.cs` (Dynamic DI Factory)
