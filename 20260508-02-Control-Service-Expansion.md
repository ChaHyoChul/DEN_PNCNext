# 모듈형 제어 서비스 확장 전략 및 통신 동기화 분석서

**작성일**: 2026-05-08  
**주제**: PncNext 프로젝트 - 기능별 서비스 분리 및 채널 공유 아키텍처

## 1. 개요
본 문서는 모션 제어 외에 레이저, 오토로더, 주변 센서 등 다양한 제어 기능이 추가될 경우를 대비한 소프트웨어 확장 전략을 정의합니다. 시스템은 기능별로 독립된 서비스를 유지하면서도, 물리적인 통신 채널을 효율적으로 공유할 수 있는 구조를 지향합니다.

## 2. 서비스 분리 및 확장 구조

### 2.1. 인터페이스 기반 모듈화
각 제어 기능은 고유의 인터페이스를 가지며, 독립적인 서비스 클래스로 구현됩니다.
- `IMotionControl`: 축 이동 및 모션 상태 관리.
- `IAutoloaderControl`: 툴 포켓 및 소재 교환 시퀀스 관리.
- `IPeripheralControl`: 스핀들, 펌프, 에어 등 주변 장치 제어.

### 2.2. 채널 주입 및 공유 (Dependency Injection)
각 서비스는 필요한 통신 채널(`IMotionChannel`)들을 딕셔너리 형태로 주입받아 사용합니다. 만약 두 서비스가 동일한 물리적 포트를 사용해야 한다면, DI 컨테이너를 통해 동일한 채널 인스턴스를 공유받게 됩니다.

## 3. 통신 자원 경합 방지 (Thread-Safety)

여러 서비스가 하나의 채널(포트)을 공유할 때 발생할 수 있는 데이터 혼선 및 충돌을 방지하기 위해 다음 메커니즘을 적용합니다.

### 3.1. SemaphoreSlim 기반 상호 배제 (Mutual Exclusion)
`ICommPort`의 구현체(`TcpCommPort`, `SerialCommPort`) 내부에 `SemaphoreSlim`을 도입하여, 한 번에 하나의 서비스만 데이터를 송수신할 수 있도록 보장합니다.

### 3.2. 원자적 트랜잭션 (Atomic Transaction)
"명령 전송 - 응답 수신" 과정이 하나의 단위로 처리되어야 하는 경우, 채널 레벨에서 락(Lock)을 획득하여 다른 서비스의 개입을 차단합니다.

## 4. 프로그램 수정 방향

### 4.1. ICommPort 및 구현체 보강
- 모든 송수신 메서드(`SendAsync`, `ReceiveAsync`)에 세마포어 대기/해제 로직 추가.

### 4.2. IMotionChannel 확장
- 특정 프로토콜에 묶이지 않은 범용 명령 전송 기능을 인터페이스에 추가하여, 서비스별 커스텀 명령을 지원하도록 개선.

## 5. 결론
이러한 확장 구조는 시스템의 응집도를 높이고 결합도를 낮추어, 새로운 하드웨어나 기능이 추가될 때 기존 코드에 영향을 주지 않고 유연하게 대응할 수 있는 기반이 됩니다. 통신 동기화 로직의 보강을 통해 다중 서비스 환경에서도 장비 제어의 안정성을 완벽히 보장할 수 있습니다.
