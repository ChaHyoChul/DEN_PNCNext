# 장비 에러 대응 및 예외 처리 메커니즘 분석서

**작성일**: 2026-04-30  
**대상**: PncNext 프로젝트 전역 에러 핸들링 및 안전 제어 모듈

## 1. 개요
덴탈 밀링머신 운영 중 발생하는 에러는 장비의 손상뿐만 아니라 작업자의 안전과 직결됩니다. 따라서 다양한 장애 상황을 즉각적으로 감지하고, 정의된 안전 시퀀스에 따라 장비를 보호하며, 사용자에게 정확한 정보를 전달하는 통합 에러 핸들링 메커니즘이 필요합니다.

## 2. 에러 유형 및 감지 조건

### 2.1. 제어기 하드웨어 에러
- **Ready 상태 중 에러**: 가공 대기 중 제어기 전원 차단, 통신 단절, 또는 자체 알람 발생.
- **가공(Run) 중 알람**: 서보 에러, 과부하, 리미트 센서 감지 등 제어기에서 실시간으로 발생하는 알람.

### 2.2. 프로세스 및 가공 에러
- **NC Feeding 오류**: 전송 타임아웃, 잘못된 G-코드 수신 등.
- **시퀀스 타임아웃**: 툴 교체나 캘리브레이션 동작이 제한 시간 내에 완료되지 않는 경우.

### 2.3. 사용자 긴급 조치
- **Emergency Stop**: 작업자가 물리적 버튼 또는 UI의 비상 정지 버튼을 누른 경우.

### 2.4. 주변 장치(Peripheral) 에러
- **Spindle 에러**: 과열, RPM 도달 실패.
- **공압/수압 에러**: 에어 압력 부족, 워터 펌프 고장 등.

## 3. 에러 처리 워크플로우 (4단계)

### 1단계: 즉각적 안전 조치 (Immediate Action)
- **모션 중지**: `IMotionControl.StopAsync()` 호출하여 모든 축의 이동을 즉시 중단.
- **주변 장치 비활성화**: 스핀들 정지, 에어/워터 펌프 차단 등 위험 요소 제거.

### 2단계: 상태 전이 및 기록 (State Transition & Logging)
- **ThreadState 갱신**: 현재 상태를 `Error`로 변경하고 에러 코드 저장.
- **로그 기록**: 에러 발생 시점의 좌표, 작업 중이던 NC 파일 라인, 상세 에러 메시지를 로그 파일 및 DB에 기록.

### 3단계: 사용자 알림 (Notification)
- **WPF/Web 전파**: 공유 메모리와 SignalR을 통해 모든 클라이언트에 즉시 팝업 또는 경고 알람 전송.

### 4단계: 복구 준비 (Recovery Preparation)
- **인터락(Interlock)**: 에러가 해제되거나 사용자가 확인하기 전까지 모든 조작 버튼 비활성화.

## 4. 시나리오별 상세 대응 전략

| 시나리오 | 감지 주체 | 핵심 대응 로직 |
| :--- | :--- | :--- |
| **비상 정지 (E-Stop)** | 전역 이벤트 리스너 | 최우선 순위로 모든 태스크 취소(`CancellationToken`), 물리적 전원 차단 확인 |
| **가공 중 제어기 알람** | `MotionMonitorService` | 가공 스레드 즉시 중단, 현재 가공 위치 저장 (재시작 시 필요) |
| **주변 장치(스핀들) 에러** | `PeripheralMonitor` | 즉시 피드 정지(Feed Hold) 후 스핀들 상태 점검, 가공 중단 |
| **통신 단절** | `CommPort` 감시 로직 | 소프트웨어적으로 `Fatal Error` 처리 후 재접속 시도 및 안전 모드 진입 |

## 5. 복구(Recovery) 메커니즘
에러 발생 후 다시 정상 상태로 복구하기 위한 절차입니다.
1.  **에러 해제 확인**: 제어기의 알람 리셋 명령 수행 및 주변 장치 상태 정상화 확인.
2.  **Home 복귀**: 기계적 원점 재설정을 통해 좌표 정밀도 확보.
3.  **상태 리셋**: `ThreadState`를 `Init` 또는 `Ready`로 변경.
4.  **작업 재개 결정**: 가공 중단 지점부터 재개(Resume)할지, 처음부터 다시 시작할지 사용자 선택.

## 6. 결론 및 제안
- **전역 예외 처리기(Global Exception Handler)**: API 레벨뿐만 아니라 백그라운드 스레드 전체를 아우르는 예외 처리 구조가 필요합니다.
- **에러 코드 표준화**: 장비, 소프트웨어, 주변 장치별 에러 코드를 체계화하여 관리해야 합니다.
- **Watchdog 도입**: 소프트웨어 자체가 멈추는 경우를 대비하여 하드웨어 또는 외부 프로세스 기반의 Watchdog 도입을 검토합니다.

이러한 체계적인 에러 핸들링 메커니즘을 통해 장비의 안정성을 높이고 장애 발생 시 피해를 최소화할 수 있습니다.
