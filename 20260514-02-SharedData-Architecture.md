# 제어기 고유 상태(State)와 공유 데이터(SharedData) 분리 아키텍처 분석

**작성일**: 2026-05-14  
**주제**: PncNext 프로젝트 - 다중 제어기 환경에서의 데이터 관리 및 매핑 전략

## 1. 개요
프로그램은 현재 PA 제어기의 응답을 `PAMotionControllerState`에 저장하고 있으며, WPF 클라이언트 등과의 실시간 통신을 위해 `MotionSharedData`라는 구조체를 별도로 운영할 계획입니다. 다중 제어기(PA, INTH 등)를 지원하는 환경에서, **제어기별 고유 객체를 거쳐 공유 데이터로 변환할지**, 아니면 **모든 제어기가 직접 공유 데이터 객체에 값을 쓸지**에 대한 아키텍처적 장단점을 분석합니다.

## 2. 아키텍처 비교

### 모델 A: 분리 및 매핑 방식 (현재 방향 / 권장)
프로토콜 파싱 로직은 제어기별 고유 상태 객체(예: `PAMotionControllerState`, `INTHMotionControllerState`)에 원시 데이터를 모두 저장하고, 중앙의 상태 관리자(State Store)가 이 중 UI나 타 프로세스에 필요한 핵심 데이터만 추려서 `MotionSharedData`로 매핑(복사)하는 방식입니다.

*   **구조**: `Protocol` -> `[제어기별 고유 State 객체]` -> `(Mapping)` -> `[MotionSharedData]`

#### 장점:
1.  **관심사의 분리(Separation of Concerns)**: 프로토콜 레이어는 하드웨어 규격대로 데이터를 해석하는 데만 집중하며, UI가 어떤 데이터를 필요로 하는지 알 필요가 없습니다.
2.  **데이터 손실 방지 및 디버깅**: PA 제어기에는 존재하지만 INTH 제어기에는 없는 고유한 하드웨어 플래그(예: 특정 밸브 상태, 보드 연결 상태 등)를 고유 State 객체에 온전히 보존할 수 있어 진단과 로깅에 유리합니다.
3.  **IPC(공유 메모리) 계약의 안정성 보장**: `MotionSharedData`는 외부 프로세스(WPF)와 메모리 레이아웃을 정확히 맞춰야 하는 Blittable 구조체입니다. 제어기별로 잡다한 데이터가 이곳에 섞이면 구조체 크기가 불필요하게 커지고, 잦은 변경으로 인해 클라이언트 프로그램이 깨질 위험이 있습니다.
4.  **정규화(Normalization)**: 각 제어기마다 I/O 포트 매핑이나 에러 코드 체계가 다를 수 있습니다. 매핑 단계를 거치면 이를 PncNext 소프트웨어의 표준 포맷으로 변환하여 `MotionSharedData`에 담을 수 있습니다.

#### 단점:
*   객체 간 데이터를 복사하는 매핑 레이어 코드가 추가로 필요합니다. (단, C#에서 이 정도의 복사 오버헤드는 성능에 전혀 영향을 미치지 않습니다.)

---

### 모델 B: 직접 할당 방식
모든 `IMotionProtocol` 구현체가 처음부터 `MotionSharedData` 구조체를 참조받아 파싱된 결과를 그곳에 직접 기록하는 방식입니다.

*   **구조**: `Protocol` -> `[MotionSharedData]`

#### 장점:
*   데이터를 옮겨 담는 중간 과정이 생략되어 코드가 단순해집니다.

#### 단점:
1.  **강한 결합도(Tight Coupling)**: 통신 프로토콜 로직이 UI 표시용 데이터 구조체에 강하게 종속됩니다.
2.  **제어기별 특화 데이터 처리 불가**: 공통 구조체에 포함되지 않은 특정 제어기만의 유용한 정보는 파싱 직후 버려지거나, 억지로 공통 구조체에 필드를 추가해야 하므로 구조체가 비대해집니다 (Blob Anti-pattern).
3.  **다중 장비 확장성의 한계**: 서로 다른 제어기들이 공통 구조체의 필드를 제각각 해석해서 사용할 위험이 있어, 결과적으로 UI에서 데이터를 처리할 때 "이 장비가 PA인가? INTH인가?"를 다시 분기 처리해야 하는 안티 패턴이 발생합니다.

## 3. 결론 및 향후 설계 가이드

다양한 하드웨어(PA, INTH 등)를 범용적으로 지원하는 덴탈 밀링머신 소프트웨어의 목적을 고려할 때, **모델 A (분리 및 매핑 방식)를 강력히 권장**합니다.

### 3.1. 제안하는 데이터 흐름 (Data Flow)
1.  **수집**: `PAMotionProtocol`은 바이트를 파싱하여 모든 원시 정보가 담긴 `PAMotionControllerState` 클래스 인스턴스에 데이터를 채웁니다.
2.  **보관**: 도메인 레이어의 `MotionStateStore` (Singleton)는 현재 연결된 장비 타입에 맞는 고유 State 객체를 안전하게 보관합니다.
3.  **추출 및 변환**: 백그라운드 서비스 또는 데이터 퍼블리셔가 주기에 맞춰 고유 State 객체에서 좌표, 주요 상태, 통합 I/O 정보를 추출합니다.
4.  **공유**: 추출된 정보만 작고 가벼운 `MotionSharedData` (Struct)에 담아 `SharedMemoryService`를 통해 WPF 클라이언트와 공유합니다.

이 아키텍처를 유지함으로써 하드웨어 통신 모듈은 각자의 규격에 충실하게 유지되고, 클라이언트 프로그램(UI)은 장비 종류에 상관없이 일관된 표준 데이터를 받을 수 있게 됩니다.
