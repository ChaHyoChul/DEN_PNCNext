# 백그라운드 상태 폴링 및 실시간 데이터 동기화 설계안

**작성일**: 2026-05-15  
**주제**: PncNext 프로젝트 - 상시 모니터링 시스템 및 데이터 파이프라인 구축

## 1. 개요
덴탈 밀링머신의 실시간 좌표 및 하드웨어 상태를 UI(WPF)에 지연 없이 표시하기 위해, 백그라운드에서 주기적으로 제어기 상태를 체크하고 이를 시스템 전체로 전파하는 자동화된 데이터 파이프라인을 구축합니다.

## 2. 핵심 구성 요소

### 2.1. MotionStatusBackgroundService (심장부)
- **역할**: 프로그램 실행 동안 100ms(초당 10회) 주기로 "상태 체크" 신호를 발생시킵니다.
- **기술**: .NET 8의 고성능 `PeriodicTimer`를 사용하여 스레드 차단 없이 효율적인 타이머 루프를 구성합니다.

### 2.2. IMotionControl & Channels (혈관)
- **역할**: 백그라운드 서비스의 요청을 받아 실제 하드웨어 명령(`RND_CDT`)으로 변환하고 통신 채널("STS")을 통해 전송합니다.
- **비동기 엔진**: 명령 전송 후 응답을 직접 기다리지 않고, 이미 가동 중인 `MotionChannel`의 수신 루프가 응답을 낚아챕니다.

### 2.3. IMotionStateStore & SharedMemory (저장 및 전파)
- **역할**: 수신된 데이터를 파싱하여 메모리에 저장하고, 즉시 공유 메모리(MMF)에 기록하여 WPF 클라이언트가 읽어갈 수 있게 합니다.

## 3. 통합 데이터 파이프라인 흐름 (Data Flow)

1.  **트리거 (Timer)**: `BackgroundService`에서 100ms 타이머가 울림.
2.  **요청 (Dispatch)**: `IMotionControl.GetStatusAsync()` 호출.
3.  **통신 (Transport)**: "STS" 채널을 통해 제어기에 상태 요청 메시지 전송.
4.  **수신 (Async Receive)**: 제어기 응답이 오면 `MotionChannel`의 백그라운드 루프가 이를 수신.
5.  **이벤트 (Notification)**: 채널이 `MessageReceived` 이벤트를 발생시킴.
6.  **파싱 및 저장 (Update)**: `PAMotionControlService`가 이벤트를 받아 데이터를 해석하고 `MotionStateStore`의 `PaState` 객체 갱신.
7.  **공유 (IPC)**: 저장소 갱신 즉시 `SharedMemoryService`를 통해 고속 공유 메모리 영역에 좌표 및 상태값 기록.
8.  **시각화 (UI)**: WPF 클라이언트가 공유 메모리를 읽어 화면에 실시간 표시.

## 4. 구현 시 고려 사항

- **에러 복구**: 특정 주기에서 통신 타임아웃이 발생하더라도 다음 주기에서 자동으로 재시도하도록 예외 처리(`try-catch`)를 강화합니다.
- **자원 효율성**: `using var scope`를 활용하여 싱글톤 서비스 내에서도 필요한 의존성을 안전하고 가볍게 해제합니다.
- **성능 최적화**: 100ms 주기는 시스템에 무리를 주지 않는 선에서 부드러운 UI 갱신을 보장하는 최적의 값으로 설정되었습니다.

## 5. 결론
이 설계안은 기존에 파편화되어 있던 통신, 파싱, 저장 로직을 하나의 유기적인 파이프라인으로 연결합니다. 이를 통해 개발자는 개별 통신 로직에 신경 쓰지 않고도 항상 최신의 장비 데이터를 안전하게 사용할 수 있는 환경을 갖게 됩니다.
