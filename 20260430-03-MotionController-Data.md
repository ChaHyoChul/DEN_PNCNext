# 모션 컨트롤러 상태 데이터 저장 및 실시간 공유 전략 분석서

**작성일**: 2026-04-30  
**대상**: PncNext 프로젝트 실시간 데이터 관리 및 클라이언트 통신 모듈

## 1. 개요
덴탈 밀링머신의 정밀한 제어를 위해 모션 컨트롤러의 상태(좌표, 속도, 에러 상태 등)를 실시간으로 모니터링하고, 이를 웹 대시보드 및 WPF 클라이언트 프로그램에서 지연 없이 공유받아 표시하기 위한 기술적 방안을 검토합니다.

## 2. 데이터 저장 전략 (In-Memory State)

백그라운드 서비스(`MotionStatusBackgroundService`)가 주기적으로 읽어온 데이터를 서버 내부에서 안전하게 보관하는 방식입니다.

### 2.1. Thread-safe Singleton 상태 객체
- **방식**: `MachineState` 클래스를 정의하고 이를 싱글톤(Singleton) 서비스로 등록하여 관리합니다.
- **특징**: 여러 스레드(백그라운드 서비스, API 컨트롤러 등)에서 동시 접근하므로 `lock` 또는 `Concurrent` 컬렉션, `volatile` 키워드 등을 사용하여 데이터 정합성을 보장해야 합니다.
- **장점**: 서버 내부에서 가장 빠르게 데이터에 접근할 수 있는 방식입니다.

## 3. 클라이언트별 데이터 공유 방안

### 3.1. Web App (React) 연동
- **REST API (Polling)**: 현재 구현된 방식으로, 주기적으로 특정 엔드포인트를 호출하여 상태를 읽어갑니다. 구현이 간단하지만 실시간성이 다소 떨어질 수 있습니다.
- **SignalR (WebSocket)**: 서버에서 상태가 변경될 때마다 연결된 웹 클라이언트에 데이터를 밀어넣는(Push) 방식입니다. 밀링 머신의 가공 진행률이나 긴급 에러 표시 등에 최적입니다.

### 3.2. C# WPF Client 연동 (Inter-Process Communication)
WPF 클라이언트가 API 서버와 별도의 프로세스로 동작할 경우, 고속 데이터 공유를 위해 다음 방식들을 검토합니다.

#### **방안 A: 공유 메모리 (MemoryMappedFiles) - 고속 공유**
- **기술**: Windows의 `MemoryMappedFile`을 사용하여 RAM의 특정 영역을 두 프로세스가 직접 공유합니다.
- **장점**: 데이터 복사 과정이 거의 없어 지연 시간(Latency)이 극도로 낮습니다. 대량의 실시간 좌표 데이터를 전달할 때 유리합니다.
- **단점**: 동기화(Mutex, Semaphore) 처리가 복잡하며, 데이터 구조(Struct)가 고정되어야 합니다.

#### **방안 B: gRPC (Local IPC) - 현대적 접근**
- **기술**: HTTP/2 기반의 gRPC를 사용하여 로컬 루프백 통신을 수행합니다. (.NET 8은 고성능 gRPC 지원)
- **장점**: 강력한 타입 정의(Proto), 양방향 스트리밍 지원, 고성능 바이너리 직렬화.
- **단점**: 공유 메모리보다는 미세하게 느리지만, 대부분의 UI 갱신 주기(16~33ms) 내에서는 충분한 성능을 냅니다.

#### **방안 C: Named Pipes**
- **기술**: Windows 표준 IPC인 Named Pipe를 활용합니다.
- **특징**: 1:1 통신에 적합하며 안정적입니다.

## 4. 권장 아키텍처 제안: 하이브리드 방식

| 구분 | 적용 기술 | 비고 |
| :--- | :--- | :--- |
| **서버 내부 저장** | **Singleton MachineState** | Thread-safe 구현 필수 |
| **Web 공유** | **SignalR** | 실시간 대시보드 및 알람용 |
| **WPF 공유** | **MemoryMappedFiles (MMF)** | 고속 좌표 갱신 및 UI 드로잉용 |

### 4.1. 공유 메모리 적용 시나리오
1. **API 서버**: `MotionStatusBackgroundService`에서 장비 상태를 읽어와 싱글톤 상태 객체를 갱신함과 동시에 `MemoryMappedFile`에 바이너리 데이터를 기록합니다.
2. **WPF 클라이언트**: 별도의 스레드에서 `MemoryMappedFile`의 특정 오프셋을 주기적으로 읽어 UI에 렌더링합니다.
3. **동기화**: `EventWaitHandle`을 사용하여 데이터가 새로 기록되었음을 WPF에 알림으로써 불필요한 CPU 점유를 방지합니다.

## 5. 결론 및 향후 고려 사항
- **실시간성 요구 수준 확인**: 좌표 갱신 주기가 10ms 이하라면 **공유 메모리**가 필수적이며, 50ms 정도의 수준이라면 **gRPC**나 **SignalR**만으로도 충분할 수 있습니다.
- **데이터 구조화**: 공유 메모리를 사용할 경우 데이터 레이아웃(Blittable Type)을 엄격하게 관리해야 합니다.
- **보안**: 공유 메모리나 IPC 채널에 대한 접근 권한 관리가 필요합니다.

이 분석 결과를 바탕으로, 프로젝트의 구체적인 실시간성 요구 사양에 맞춰 최적의 IPC 기술을 선택하여 구현할 예정입니다.
